"""Reusable, dependency-light feature extraction helpers.

Detectors call into these. Keeping the math here (rather than inside each
detector) makes the signal logic easy to read, test, and reuse.
"""
